import 'package:flutter/material.dart';

class ManageProducts extends StatelessWidget {
  static String id = 'ManageProducts';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('manage products '),
        ),
        body: Column(
          children: [Text('aliiiii ')],
        ));
  }
}
